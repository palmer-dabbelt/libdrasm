LANGUAGES   += c++
COMPILEOPTS += -Wall
COMPILEOPTS += -Wextra
COMPILEOPTS += -Werror
COMPILEOPTS += -std=c++0x
COMPILEOPTS += -pedantic

LANGUAGES   += bash

LANGUAGES   += h

LANGUAGES   += pkgconfig
COMPILEOPTS += -Sobj/proc/version-pc.sed

GENERATE    += version.h
GENERATE    += version-pc.sed

LIBRARIES   += libdrasm.so
COMPILEOPTS += `ppkg-config libocn --cflags`
LINKOPTS    += `ppkg-config libocn --libs`
SOURCES     += libdrasm/machine.c++
SOURCES     += libdrasm/tile.c++
CONFIG      += find_library_headers

LIBRARIES   += pkgconfig/libdrasm.pc
SOURCES     += libdrasm/libdrasm.pc
